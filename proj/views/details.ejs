<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mu-G | <%= title %></title>
    <%- include('./style.ejs')%>
    <link rel="stylesheet" href="/style.css">

</head>
<body>
   <nav>
    <div class="site-title">
    <a href="/"><h1>Mu-G</h1></a>
     <p>LoginPage</p>
    </div>
    <ul>
        <li> <a href="/">Login Page</a> </li>
        <li><a href="/admin-log">Admin Page</a> </li>
        <li><a href="/newblog"> New Blog</a> </li>
    </ul>
   </nav>
   <div class="create-blog content">
    <form action="/blogs/:id" method="post">
        <input type="text" name="name" id="name" value="<%=blog.name%>" required>
        <input type="email" name="email" id="email"value="<%=blog.email %>" required>
        <input type="text" name="password" id="psswd"value="<%=blog.password %>" required>
        <input type="hidden" name="id" value="<%=blog._id %>">
    
        <button>
           Edit
        </button>

    </form>
        
       </div>

   <footer>   
    <p>copyright &copy; Blog Mu-G</p>
</footer>
<script>

// let pen = document.querySelector('a.Edit');
//          pen.addEventListener('click',(e)=>{
//             let endpoint = `/blogs/${pen.dataset.doc}`;
//             fetch(endpoint,{
//                 method:'POST'})

//         .then((response)=>response.json())
//            .  then((data)=>window.location.href = data.redirect)
//          .catch((err)=>console.log(err))
//          })

    let trashcan = document.querySelector('a.delete');
         trashcan.addEventListener('click',(e)=>{
            let endpoint = `/blogs/${trashcan.dataset.del}`;
            fetch(endpoint,{
                method:'DELETE'})

        .then((response)=>response.json())
           .  then((data)=>window.location.href = data.redirect)
         .catch((err)=>console.log(err))
         }); 


       
</script>
</body>
</html>